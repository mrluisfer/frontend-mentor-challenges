---
import { ChevronLeft } from "lucide-react";
import PageTitle from "../PageTitle";
import ChallengeNavigation from "./ChallengeNavigation";
import { challengesInitialState } from "@/constants/challenges";
import { clearChallengeRoute } from "@/utils/clearChallengeRoute";

interface Props {
	challengeName: string;
}

const { challengeName } = Astro.props;

const formattedChallengeName = challengesInitialState.find((challenge) => {
	return clearChallengeRoute(challenge.route) === challengeName;
})?.title ?? challengeName.replace(/-/g, " ");
---

<header class="py-6 flex items-center justify-between px-6">
	<a
		class="p-1 flex items-center gap-2 justify-center hover:bg-neutral-100 rounded-md transition bg-white"
		href="/"
		title="Go to home"
	>
		<ChevronLeft />
		<h1 class="font-extrabold text-2xl">
			<PageTitle />
		</h1>
	</a>
	<p class="ml-4 max-w-[8.5rem] truncate text-sm font-semibold sm:max-w-[11rem] sm:text-base md:max-w-none md:text-lg">
		{formattedChallengeName}
	</p>
	<ChallengeNavigation challengeName={challengeName} client:only />
</header>
